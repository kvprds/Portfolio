# OS cruft
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
Thumbs.db
desktop.ini

# Editors
.vscode/
.idea/
*.swp
*.swo
*~

# Secrets — nothing here needs them, but never let one slip in
.env
.env.*
*.pem
*.key

# Cloudflare / Wrangler, if you ever add a wrangler.toml
.wrangler/
.dev.vars

# This site has no build step and no dependencies. If either of these
# ever appears, something has gone wrong with the "static, no npm" rule.
node_modules/
dist/
